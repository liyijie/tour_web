<%= form_for(@tour_order) do |f| %>
  <% if @tour_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tour_order.errors.count, "error") %> prohibited this tour_order from being saved:</h2>

      <ul>
      <% @tour_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
