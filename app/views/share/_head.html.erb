<nav class="navbar navbar-default navbar-static-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <%= link_to(image_tag("logo.png"), root_path, class: "navbar-brand") %>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapsed">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div id="navbar-collapsed" class="collapse navbar-collapse">
    	<ul class="nav navbar-nav">
    		<li id="navbar-home" class="<%= "active" if @mode.to_s == "" %>">
          <%= link_to("首页", root_path) %>
        </li>
        <li id="navbar-line" class="dropdown <%= "active" if @mode.to_s == "pomotion" %>">
          <%= link_to('#' , "data-toggle" => "dropdown", class: "dropdown-toggle") do %>
            精品主题<span class="caret"></span>
          <% end %>
          <% categories = Category.all %>
          <ul class="dropdown-menu" role="menu">
            <% categories.each do |category| %>
              <li><%= link_to(category.name, category) %></li>
            <% end %>
          </ul>
        </li>
        <li id="navbar-city" class="dropdown <%= "active" if @mode.to_s == "city" %>">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">热门城市<span class="caret"></span></a>
          <% cities = City.all %>
          <ul class="dropdown-menu" role="menu">
            <% cities.each do |city| %>
              <li><%= link_to(city.name, city) %></li>
            <% end %>
          </ul>
        </li>
    	</ul>
    </div>
  </div>
</nav>
<div class="container-fluid bg-blue">
  <div class="container">
    <ul class="nav nav-tabs menu-top" role="tablist">
      <li role="presentation" class="active"><a href="#">首页</a></li>
      <li role="presentation"><a href="#">贵阳</a></li>
      <li role="presentation"><a href="#">遵义</a></li>
    </ul>
  </div>
</div>
